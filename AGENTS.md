# Project Agent Instructions

Read `START.md` and `.project.json` before substantial work.



Keep changes scoped here. Preserve user changes and inspect Git before editing. Do not commit, push, publish, or create external resources unless explicitly requested.

When project state materially changes, update `.project.json` and regenerate the central registry with the `project-bootstrap` skill.
